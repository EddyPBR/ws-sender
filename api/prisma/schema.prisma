generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Phones {
  id    String @id @unique @default(uuid())
  name  String
  phone String @unique

  @@map("phones")
}
